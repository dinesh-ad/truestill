# (abz) Organize shows one population three ways and connects none of them.

> ❌ **RETIRED 2026-09-02 (P190). The operative clause was false before this was filed**: `app.js:rearrangeNote`
> (`93635af`, 2026-08-06) prints *"N of M files here are already in your library"* on exactly the re-preview this
> entry describes. Four framings exist, not three, and the surviving observation is a duplicated headline.

*Body of retired backlog entry `(abz)`, a record. The index is [`BACKLOG.md`](../../BACKLOG.md); the letter namespace is shared with [`SHIPPED.md`](../../SHIPPED.md).*

- **(abz) Organize shows one population three ways and connects none of them.** Recorded
  2026-08-08, editorial. On one flow: *"2109 photos and videos here"* (`app.js:validatePath`, from
  `/api/fs/validate`), then *"2,106 photos - 3 videos found"* (the preview summary, split by
  kind), then *"2,109 duplicates"* (`app.js:organizeTally`, `s.exact_dup`). `2106 + 3 = 2109`: the SAME
  files each time, framed first as a location count, then a composition, then a verdict. Nothing
  says so, so the reader must infer that the third number is the first one again rather than a
  new pile of 2,109 things.
