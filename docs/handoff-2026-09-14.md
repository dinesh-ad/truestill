# Handoff - 2026-09-14

**A dated state check.** Numbers here are readings taken today, and each carries the command or
the `file:line` that produced it. Earlier handoffs are records and are not superseded by this one -
[`handoff-2026-09-07.md`](handoff-2026-09-07.md) is the previous start point, and §6 lists what in
it no longer holds. Where something is not known it says **unestablished**; that is a finding.

⚠ **The week after 09-07 moved the product, not only Organize.** Licensing stages, recover end to
end, Takeout apply, real-drive reach, CI/typecheck, a frontend contract for a tool that cannot
ask, and a Cursor UI pass on Backups and the account row. Read §4 and §7 before picking the next
slice - several open defects are custody-surface lies, not polish.

---

## 1. State

| reading | value | command |
|---|---|---|
| HEAD | `5b8a266`, **pushed** | `git rev-parse HEAD` |
| `origin/main` | `5b8a266` - matches HEAD | `git ls-remote origin refs/heads/main` |
| push run on HEAD | `34809964875` on `5b8a266`, `success`; ubuntu / macOS / windows check all `success`; `e2e` skipped by design | `gh run view 34809964875 --json jobs` |
| last CI browser lane | run `34747406781`, nightly `schedule`, on `409ddae`, `e2e (chromium, ubuntu)` and `e2e (webkit, ubuntu)` both `success` | `gh run view 34747406781` |
| commits CI's browser lane has never seen | **2** (`409ddae..HEAD`): `d73d1d2` (Cursor UI pass), `5b8a266` (D18–D20 docs) | `git rev-list --count 409ddae..HEAD` |
| `make check` | `3944 passed, 3 skipped, 1 xfailed in 46.61s` wall **115.25 s** on `5b8a266` | `/usr/bin/time -f 'CHECK %e' make check` |
| `make typecheck` | **67.16 s** wall - **58%** of that `make check` (115.25 s); no `TEST_SECONDS_MAX` wrap | `/usr/bin/time -f 'TYPECHECK %e' make typecheck`; `Makefile:129` ceilings only the pytest lane |
| open entries / Build next | 130 / 14 | `grep -cE '^- \*\*\([a-z]{1,3}\)' docs/BACKLOG.md`; same over `## Build next` |
| tests collected | 3,948 for `make check`; 633 for `tests/e2e` on one collect (CI runs chromium and webkit as two jobs) | `uv run pytest --collect-only -q -o addopts=""`; `uv run pytest tests/e2e --collect-only -q -o addopts=""` |
| tags | `v0.1.0`, `v0.1.1` | `git ls-remote --tags origin` |
| tracked `.md` / backlog bodies | 335 / 247 | `git ls-files '*.md' \| wc -l`; `git ls-files 'docs/research/backlog/*.md' \| wc -l` |

⚠ **A week of frontend work did land, and most of it has been through a browser lane** - the
nightly on `409ddae` includes Organize follow-ups, brand plate, account slot, Import UI, Backups
card copy, and the contract docs. What has **not** is the Cursor pass (`d73d1d2`) that changed
the account row, Backups canvas treatment, search-size control, and brand/mesh motion. Docs-only
`5b8a266` is in the same unread window.

⚠ **`make e2e-fast` on HEAD is unestablished today** - not re-run for this handoff. Local green on
`5b8a266` is `make check` only.

## 2. What landed since 09-07, in order

The arc runs `fa85dac..HEAD`, **52 commits** (`git rev-list --count fa85dac..HEAD`), including the
09-07 handoff itself (`dfe2e35`). Grouped by what moved; each group names what it did **not** do.

1. **`dfe2e35` - handoff 2026-09-07.** Docs only. Did **not** change a pixel or a gate.
2. **`7a1869d` … `48c5316` - Organize follow-ups and brand.** First-run question above modes
   (`5b9efd4`), posix destination tree on Windows (`4d533da`), stepper/summary/counts/frame
   (`5c27d4a`–`be04814`), plate replaces reconstruction and type scale shrinks (`eb392e7`,
   `6386405`), path fields drop to secondary type (`ecec6da`). Did **not** convert the six other
   screens; `(akj)` filed that no lane executes `app.js` on Windows (`752295c`).
3. **`1f7f528` … `50e988d` - licensing stages.** D16 (`1f7f528`); token + verifier (`334c0f9`);
   cumulative cap (`ee85f4e`); one message / exit code (`e7b31e3`); account slot on the rail
   (`c6ddd9c`–`e1b7a67`); customer store + mint audit (`747c94c`, `e4ff84c`); free tier joined on
   both surfaces (`50e988d`). Did **not** build payment, a licensing server, or a sell screen.
4. **`3a6452e` … `ac992dc` - recover end to end.** Stage 1 carried counts (`3a6452e`); CLI
   `recover` (`bb65e08`); app drive-card restore (`ac992dc`). Did **not** cap recover (D16 §7).
   **`1867be3`** widened a CI artifact census from `git ls-files` to `--others` so untracked
   outputs stop escaping the guard - see §8.
5. **`09e3559` … `8b53123` - Backups cards (pre-Cursor).** Library name, counts, leading number,
   qualifier. Did **not** close the drive-card `"last checked: never"` half of `(aes)` - §4.
6. **`bf1b503` … `efe9ec3` - Takeout import applying.** D17 + destination-scoped preview
   (`bf1b503`); apply route and rail width (`0d1f1c4`); confirm copy and completion numbers
   (`1e769de`); staging copy leaves with the import (`efe9ec3`). Did **not** ship `--tz` /
   `--prefer-takeout-dates` / `--map-albums` (D17 status).
7. **`8894e1e` … `28d9e0c` - e2e matrix split and CI honesty.** Ceiling / junit designation /
   two legs one per engine. Did **not** put the browser lane back on every push.
8. **`14f369a` … `9b252dc` - real-drive / verify.** Verify reads the medium not the page cache
   (`14f369a`); two ejected-drive findings filed with no work attached (`9b252dc`).
9. **`4759efd` … `7d55f92` - platform mypy gap.** `posix_fadvise` bind so the guard type-checks
   off Linux (`4759efd`); Path compares (`5e43d7b`); source mypy loops every CI platform including
   darwin (`7d55f92`). **Two red pushes** on the way - §8.
10. **`4e1305a` - absent-drive reach field.** Every surface that names a drive says whether it is
    there. Did **not** invent a new drive model.
11. **`a66cf90` … `409ddae` - doc pass + frontend contract.** Fourteen stale claims corrected;
    `FRONTEND_CONTRACT.md` written and corrected twice (two spacing scales; renamed-id claim
    measured); map row went stale in one hour (`409ddae`). Did **not** change product behaviour.
12. **`d73d1d2` - Cursor UI pass.** Quiet account row (`<details>` for path/picker/sign-out);
    Backups canvas / at-risk as inventory; discrete search-size slider; CSS brand/mesh motion.
    Did **not** redesign Events, Find, Stats, Settings, or Import layout; did **not** touch the
    Organize form island cutover; **never been through CI's browser lane** (§1).
13. **`5b8a266` - D18, D19, D20 recorded.** Yearly subscription with perpetual fallback; warm
    canvas restored; rose-charcoal rail documented. Did **not** change token hexes (already on
    disk); living-doc sweep only.

## 3. What the product looks like now

**Seven screens** (`grep -oE 'id="screen-[^"]+"' …/templates/index.html`):

| id | line | designed? |
|---|---|---|
| `screen-organize` | `index.html:169` | Yes - 09-07 rebuild + follow-ups; four React mounts (`main.tsx:595-598`) |
| `screen-backups` | `:402` | Partially - card copy and Cursor canvas/at-risk pass; not a full redesign |
| `screen-import` | `:372` | Apply path shipped; layout still shared-pattern, not Organize-depth |
| `screen-events` | `:340` | Shared pattern / type pass only |
| `screen-find` | `:537` | Shared pattern / type pass only |
| `screen-stats` | `:559` | Shared pattern / type pass only |
| `screen-settings` | `:567` | Not even on the shared pattern (`ui-inventory.md:136-138`) |

**What Cursor changed (`d73d1d2`):** account slot resting row; Backups at-risk band vs inventory;
search text-size as five discrete stops; ambient mesh + brand-alive CSS (`app.css:30-56`,
`:302-365`). **What it did not touch:** Organize form bindings, the six-screen composition gap,
React cutover of typed-confirm / undo / progress (`(aki)`).

**Canvas** - warm again (**D19**): `#faf8f5` → `#f1ebe1` → `#e8e9f3` (`tokens.css:94-96`).
`test_palette_and_resting_panel.py:86` refuses flat `#ffffff`.

**Rail** - rose-charcoal `#161014` (**D20**, `app.css:146`); `--rail-accent` **9.93:1** on that
ground (`app.css:158`).

**Licence row** - `#account-slot` (`index.html:134-145`), filled by `renderAccount`
(`app.js:4959-5022`) from `/api/account`. Compact resting row after `d73d1d2`; path/picker/sign-out
behind `<details>`.

**Animation** - CSS-only mesh drift and brand stop motion; reduced-motion respected in the same
blocks. Whether the motion matches a maintainer reference is **unestablished**.

## 4. What is open, and why it matters

- **Six screens with no Organize-depth design.** Events, Find, Stats, Settings, and most of
  Import/Backups composition. Shared tokens and a type pass are not a design
  (`handoff-2026-09-07.md:197`, still true; `ui-inventory.md:134-138`).
- **Two spacing scales that diverge 25% when text size changes.** `--space-*` is px;
  Tailwind's unaliased `--spacing` is rem. They agree at the shipped root size and are **25% out**
  at `small`/`large` (`FRONTEND_CONTRACT.md:350-382`, `:462-463`).
- **Three mode-badge icons with no provenance row.**
  `LICENSE-icons.txt:46-66` (*UNCOVERED - THREE ORGANIZE MODE BADGES*); used at
  `index.html:270,278,286`.
- **`(ako)` - three deletes that diverge from the re-read standard.**
  `docs/research/backlog/ako.md`: reclaimable bypass, migrate resume delete, NULL `copy_sha256`
  existence-only gate. Fifteen other paths re-read bytes first; these three do not.
- **Backups drive card: `last checked: never` after a verify found gaps.** Stats table was fixed
  by `(aes)` via `was_checked` (`app.js:724`). Drive cards still use
  `neverChecked = !d.last_verified` and render `last checked: ${checked}` (`app.js:3166-3193`);
  `DriveRow` has no `was_checked` (`service/drives.py:510-552`). A gap-finding verify with NULL
  `last_verified` still reads as never checked on the card a person uses to act.
- **Typecheck has no ceiling and is most of `make check`.** `TEST_SECONDS_MAX` wraps only pytest
  (`Makefile:129`, `:201`). Measured today: typecheck **67.16 s** of check **115.25 s** (**58%**).
  The three-platform mypy loop (`7d55f92`) is why.
- **The app is English-only with no translation system.** No `gettext`, no `i18n/` tree; v2
  research recorded *"no i18n"* (`ui-v2-research.md:411`). **Unestablished** as a binding
  `DECISIONS.md` ruling - it is practice and a research record, not D-numbered.
- **Carried from 09-07 and still open:** `(aki)` shared-widget wall; receipt still cannot group by
  destination folder on completion; D4 counts that disagree; Organize form still last for React
  while typed-confirm owns submit.

## 5. The decisions made this week, in one place

| id | date | one line | where |
|---|---|---|---|
| **D16** | 2026-09-11 | Free tier is a volume cap; lapsed licence loses nothing; enforcement is a speed bump; recover never capped | `DECISIONS.md:964` |
| **D17** | 2026-09-11 | Import applies in the app; means into this destination | `DECISIONS.md:1288` |
| **D18** | 2026-09-14 | Yearly subscription with perpetual fallback; $59/yr; annual only (supersedes D6 §1 pay-once) | `DECISIONS.md:1360` |
| **D19** | 2026-09-14 | Canvas warm again `#faf8f5` → `#f1ebe1` → `#e8e9f3` (supersedes 2026-09-06 white) | `DECISIONS.md:1414` |
| **D20** | 2026-09-14 | Rail rose-charcoal `#161014` | `DECISIONS.md:1445` |

**Standing process rule:** nothing is treated as done until **all three** check lanes (ubuntu,
macOS, windows) report success. Written into `Makefile:64-65` and enforced by the pre-push
`push-gate` (`scripts/check_push_gate.py`). Two red builds this week (`9b252dc`, `4759efd`) are
why - §8.

## 6. What is stale in `handoff-2026-09-07.md`

⚠ **That file is a record. Nothing below rewrites it.** Each row is a claim that was true on
2026-09-07 and is false against today's tree, with the contradicting cite.

| 09-07 claim | contradicting cite today |
|---|---|
| `:18` HEAD `fa85dac`, **unpushed** | HEAD `5b8a266`, pushed (`git rev-parse` / `git ls-remote`) |
| `:22` browser lane last on `3b97d1d`; 17 commits unseen | last success on `409ddae`; **2** unseen (`git rev-list --count 409ddae..HEAD`) |
| `:25` `3585` passed / 40.43 s | `3944` passed / 46.61 s pytest, 115.25 s wall `make check` |
| `:28` 126 open / 327 `.md` / 241 bodies | 130 / 335 / 247 (commands in §1) |
| `:70-72` canvas goes white | warm gradient `tokens.css:94-96`; **D19** `DECISIONS.md:1414` |
| `:115-116` plate measured against `#17150f` | rail ground `#161014` `app.css:146`; **D20** |
| `:227-229` *"The canvas is white"* / all `#ffffff` | same as above - **D19** |
| `:8` *"nothing else moved"* after Organize | §2 - licensing, recover, import apply, reach, CI, Cursor UI |
| `:197` *"six other screens are untouched"* (as of that day) | Backups/Import/account row moved since; composition depth still mostly true |
| `:274-279` first thing = push then dispatch | HEAD is pushed; remaining unread window is `d73d1d2`+`5b8a266` |

`app.js` and `index.html` line numbers throughout 09-07 have moved again; treat every
`app.js:NNNN` there as a historical pointer, not a live one.

## 7. The first thing to do next

**Dispatch the browser lane on `5b8a266` (`gh workflow run` / `workflow_dispatch`), then fix the
Backups drive-card `"last checked: never"` half of `(aes)`.**

Why that order: two commits have never been through CI's browsers, and one of them (`d73d1d2`)
changed surfaces the lane asserts. Local `make check` cannot see them. After the lane is green
(or red with a name), close the drive-card gap - stats already tells the truth (`app.js:724`) and
the card a person acts on still does not (`app.js:3166-3193`). That is a custody lie on the
restore screen's neighbour, not a visual preference.

Do **not** start another screen redesign before the lane has seen the Cursor pass - same class as
09-07 §8's warning, one week later.

## 8. Process findings (the reusable half)

1. **A claim went stale in sixty minutes.** `409ddae` - the CLAUDE.md map row for the frontend
   contract said *"type is rem, spacing is px"* for one hour on 2026-09-12 while the contract
   itself had already found two scales (`CLAUDE.md:128`; commit message of `409ddae`).
2. **Guards blind to untracked files.** `1867be3` - a census that used `git ls-files` could not
   see the commit it was gating; widened to `--others` (`.gitignore:36-38` comment;
   `test_every_artifact_ci_writes_is_designated.py:10`).
3. **`getComputedStyle` mid-theme returns the wrong theme.** When the OS prefers dark and Light
   is chosen, any token omitted from `[data-theme="light"]` keeps its media-dark value while
   neighbours flip - measured on Backups 2026-09-13 at 1.12:1 / 1.04:1
   (`test_server.py:514-521`; `tokens.css:431-434`). e2e waits 120 ms after `emulate_media`
   before reading (`test_palette_and_resting_panel.py:92-93`) for the same class of race.
4. **Two red builds from pushing before macOS and Windows reported.**
   - `9b252dc` run `34697176552`: ubuntu success, **macOS failure**, **windows failure**.
   - `4759efd` run `34697535748`: ubuntu + macOS success, **windows failure**.
   The standing rule in §5 is the response; the push gate now refuses a red tip
   (`scripts/check_push_gate.py`).
