# Handoff - 2026-09-05

**A dated state check.** Numbers here are readings taken today, and each carries the command or
the `file:line` that produced it. Earlier handoffs are records and are not superseded by this one -
[`handoff-2026-09-04.md`](handoff-2026-09-04.md) is yesterday's, and §7 below lists what in it no
longer holds. Where something is not known it says **unestablished**; that is a finding.

---

## 1. State

| reading | value | command |
|---|---|---|
| HEAD | `c40607f`, pushed, `origin/main` identical | `git rev-parse HEAD; git rev-list --left-right --count origin/main...HEAD` |
| push run on HEAD | `33990869905`: `check` ubuntu, macos, windows all `success`; `e2e` skipped by design | `gh run list --commit "$(git rev-parse HEAD)"` |
| last browser-lane verdict | run `33955792599` on `08a73b4`, dispatch, `1011 passed, 3 skipped in 1306.44s`, both engines; **four commits since**, none seen by CI's lane | `gh run list --workflow ci.yml --event workflow_dispatch --limit 1` |
| `make check` | `3573 passed, 3 skipped, 1 xfailed in 36.98s` on `c40607f` | `make check` |
| `make e2e-fast` | `1011 passed, 3 skipped in 315.58s`, wall 318 s, on `0260715`, both engines. **Not re-run on the four commits after it**; `1061480` touched the Makefile, `ce59f18` a test, `c40607f` the preview service and two card strings | `make e2e-fast` |
| open entries / Build next | 124 / 14 | `grep -cE '^- \*\*\([a-z]{1,3}\)' docs/BACKLOG.md` |
| tests collected | 3,577 for `make check`; 1,014 for the lane on both engines | `uv run pytest --collect-only -q -o addopts=""`; the same with `tests/e2e --browser chromium --browser webkit` |
| tags | `v0.1.0`, `v0.1.1` | `git ls-remote --tags origin` |
| tracked `.md` / backlog bodies | 324 / 239 | `git ls-files '*.md' \| wc -l` |

`make e2e` serial measured 1593 s and 1653 s today on 16 cores (`Makefile:199-202`), and the same
lane on CI's 4-core runner 1306 s and 1476 s (`gh run view 33955792599`, `33947262070`).

## 2. What landed today, in order

1. **`9c29c50` - a run gate reads the completion marker, not prose.** Three e2e gates waited on
   words the preview card already showed; they now wait on `.done-mark`, emitted only by
   `completionCard` (`app.js:587`). Two page-driven tests hold the class in
   `tests/e2e/test_the_preview_does_not_pre_satisfy_the_run_gate.py`. Did **not** change the
   marker or any app behaviour.
2. **`26818b4` - the pin moves into `make check`**
   (`packages/truestill-app/tests/test_run_gates_read_the_completion_marker.py`), the `DriveBusy`
   sentence loses its article (`jobs.py:344`, "Out is busy: organize is already running."), §4 gains
   the gate-versus-assertion member. Did **not** touch any e2e assertion; four tests assert the
   operation name lowercase and were left exactly as written.
3. **`08a73b4` - `(akf)`**, `BACKLOG.md:643`: the lane's 26-28 minutes serial against "5:38"
   recorded for `-n auto`. No work attached.
4. **`7b8a908` - `make e2e-fast` and `make e2e-loop`** (`Makefile:204-215`): the same recipe under
   `-n auto`, both engines in 289 s, chromium alone in 95 s, measured on 16 cores. Did **not** change
   `make e2e`, which stays serial because it is CI's shape on 4 cores; `ci.yml:464-469` records that
   the "budget exhausted" reading of the red `-n auto` dispatch is unestablished.
5. **`8c76e2f` - the inventory card is drawn by the island** (`frontend/src/inventory.tsx`), first
   React content inside `#org-result`. `renderInventoryResult`, `renderUnreadable`, `byFormat`,
   `renderSkippedDetails` deleted from `app.js`. Did **not** change a rendered byte beyond inline
   style serialisation; DOM compared before and after.
6. **`0260715` - the dedup preview card is drawn by the island** (`frontend/src/preview.tsx`).
   Twelve builders deleted from `app.js`; every decision - the `!s.files` branch, the
   `will_organize` gate, `pointsAtRearranging`, the skip-undated form read - stays in `app.js`
   as `previewView` (`app.js:2542`). Did **not** move `renderOrganizeRunConfirm` or `#org-confirm`.
7. **`1061480` - `make look`** (`Makefile:176`): the app on `/data/TruestillLibrary/look`, 261 real
   files organized once with their own catalog, port 7358. Did **not** build any browser
   automation, and no test may read that folder.
8. **`ce59f18` - the D2 regression test**, red on purpose:
   `test_the_preview_promise_equals_the_run.py:139`, promised 0 against organized 3 into a fresh
   second destination.
9. **`c40607f` - the one-resolve fix**, `service/organize.py:1036-1105` and `DECISIONS.md:819`
   (D14): the preview resolves once, catalog-global, and `_promise_view` re-judges each twin
   against `_scope_to_marker(destination, catalog)`. Did **not** scope the resolve itself - see §3
   D2 for why that was wrong - and did **not** touch the run path or the CLI.

## 3. The five defects found by using the product

Filed by the maintainer from `make look`, each verified against the code before anything moved.

**D1 - stale result on source change. Open. Small.** Typing a new folder into `#org-source`
updates only the hint, because the two path fields are wired to `validatePath` alone
(`app.js:2080-2088`, `:2005-2018`). The only reset of `#org-result` is the next Look-inside click,
`organizeResult.set({ kind: "resting" })` at `app.js:2578`; nothing on `input` or `change` touches
`#org-result`, `#org-why`, `#org-confirm` or `#panel`. The typed confirm survives too, so a stale
"Organize N files" button runs whatever the fields now hold. Same for the destination field. No
test types a new folder over a result.

**D2 - no second copy. Fixed in `c40607f`. ⚠ The one-argument fix was WRONG and must not be
retried.** Passing `on_destination=_scope_to_marker(...)` into the preview's single `resolve` made
the promise true and emptied the naming: `_matched_drives` reads `exact_duplicate.origin ==
CATALOG`, and `organizer._scope_to_destination` (`organizer.py:918-920`) returns no match for a twin
absent from this destination, so four cases of `test_the_preview_names_the_drive.py` went red.
Two rulings both hold - `(aei)` for the promise, `IMPLEMENTATION_STANDARDS.md:1557` for the
naming - and they need two answers from one pass, which is what D14 records. Proved by mutating
the re-judgement: "every twin is here" reddens the promise; "no twin is here" is caught by
`test_overlapping_organize_runs.py` and `test_preview_tally_is_disjoint.py`, not by the promise
file, whose cases hold no twin on the destination.

**D3 - invisible refusal. Open. One line plus a tone question.** Look inside requires a destination
at `app.js:2572-2575`, but the call it guards posts `{ source }` alone (`app.js:2580`), and the
route reads only `body["source"]` (`server.py:266-270`). The gate arrived with `a062ffc` and no
reason is recorded. It is the same check the duplicate step genuinely needs (`app.js:2604-2607`).
The refusal is written by `setWhy` into the help-text span (`index.html:220`), so it looks like a
hint. No test asserts the gate.

**D4 - counts that disagree. Naming, open; the 157-versus-161 half was D2.** "N photos and videos
here" is `fs_validate`'s recursive extension count (`fs_browse.py:180-232`); "N photos · M videos
found" is the inventory walk by extension (`service/organize.py:710-725`); the panel's count is the
catalog (`service/drives.py:774`, `:800`). Three quantities, three names, one screen. The promise is
`ReportBuckets.will_organize` (`models.py:607`); the organized count is `_completion`'s status
tally (`service/organize.py:1621`, `:1703-1710`).

**D5 - the broken sentence. Open. One line.** `LIBRARY_REDUNDANCY[NOT_INDEPENDENT]`
(`drive.py:235-237`) is a fragment its caller must finish, per the comment at `:230-235`. The strip
finishes it with "." at `app.js:1937` and then appends `age`, which starts with ", " at
`app.js:1931`, so ".," appears whenever `not_independent` and `never_checked_drives` coincide.
`test_custody_strip.py` never sets both. The class - fragments punctuated at different sites -
also lives at `app.js:1008`, `:2256`, `:3138`, `:3820`, `:4023`; the backup card at `:1379` and
`cli.py:2051` finish the same fragment correctly.

## 4. The React cutover, piece by piece

**React today, inside `#org-result` only**: the inventory card (`inventory.tsx`), the dedup preview
card (`preview.tsx`), and the completion card's host, which still injects a string `app.js` builds
(`main.tsx:176-185`). **Not React**: the form (`index.html:132-229`), `#org-confirm` and the typed
word (`app.js:2169`), `#org-undo-panel`, the progress block cloned from `tpl-run`
(`index.html:501`), the `#panel` aside (`index.html:473`, `app.js:renderPanel`), the completion
card's content (`app.js:organizeCompletion`), and all six other screens.

**The seam**, `window.truestillMarkup`, exists only because `app.js` still composes strings. Its
callers, `app.js:1112-1113` (`chips`, `legend`, the completion card), `:2829` (`byFormat`, Backups'
drive list), `:3139-3141` (`dateQuality`, `inferredShifts`, `matchList` ×2, the Import preview).
Three moves remove it: the completion card into the island, the Import preview card, then
Backups' drive list or a one-line formatter for it. The formatters seam runs the other way,
`window.truestillFormatters` (`app.js:652`), and stays as long as `fmtBytes` has one home.

**The form goes last, and the reason is parse time**: `app.js` binds `$("org-preview").onclick`
at `:2567`, `$("org-dedup").onclick` at `:2595`, the radios at `:2713` and the library-root save
at `:3889` when it is parsed, and it loads before the bundle (`index.html:519`, `:523`). A React
form does not exist yet when those lines run.

**`react-migration-plan.md` still says Organize goes last** at `:303` and `:335`, in the 2026-08-14
record, and the maintainer ruled otherwise at `:143-155`: Organize is the only screen with a
design, so converting another first builds a look that gets rebuilt. Two commits per slice,
renderer then appearance, because 19 e2e files assert computed styles.

## 5. What is open, and why it matters

- **The tally says "new" for files the library holds** (`preview.tsx:271`, from the promise view).
  True of the destination, false of the library; below the rearrange threshold
  (`app.js:2514`, floor 20, ratio 0.8) no banner explains it.
- **`exact_dup` and `exact_dup_matches.total` can differ** (`service/organize.py:1055`, `:1058`),
  0 against 3 for a fresh destination. Nothing asserts they agree.
- **The rearrange pointer counts the library's view** (`exact_dup_matches.already_in_library`,
  `app.js:2544`) while the promise counts the destination's. For a destination holding some of the
  files the two numbers describe different sets.
- **The receipt redesign, grouped by destination folder, needs a payload field that does not
  exist.** `_completion` sends per-file only `sha256`, `name`, `w`, `h` (`service/organize.py:1498`)
  and `folders` by category label (`:1642`, `:1670`). `Decision.relative` (`models.py:531`) knows
  the folder and reaches the catalog, not the payload. Backend change, small.
- **Videos have no thumbnail at all.** `thumbnails.render` (`thumbnails.py:286`) is PIL, and
  `thumbs.thumbnail_bytes` (`thumbs.py:134`) raises on a decode failure; the grid's photo filter
  (`service/organize.py:1653`) is what stops 42 broken tiles. A video tile is a new capability.
- **The 48-tile sample is run order** (`service/organize.py:1691-1696`, `GRID_SAMPLE_LIMIT` at
  `:125`), so a 3,783-photo run showed one dinner. The grid itself holds up: 48 tiles, 518 KB,
  60 fps, measured in `/data/TruestillLibrary/look-2026-09-05/shots/`.
- **The Import screen resolves unscoped in preview and run** - `(akg)`, `BACKLOG.md:1296`, whether
  defect or semantics unestablished.

## 6. What the maintainer decided today, so it is not re-asked

- Icons stay inlined, with per-icon provenance in `static/LICENSE-icons.txt`; `lucide-react`
  stays out.
- Organize converts first; the other six screens are untouched until it is done.
- The receipt is redesigned grouped by destination folder - yes; its payload is §5.
- The panel gives up its width to the photographs.
- Correctness before appearance: the renderer swap lands green on the unchanged suite before any
  look changes.
- The soak folders were deleted on his confirmation; `/data/TruestillLibrary/soak-deleted-2026-09-05.txt`
  is the record.

## 7. What is stale in yesterday's handoff

- `handoff-2026-09-04.md:61`, "`make e2e -n auto` is **5:15**": no run reads 5:15
  (`BACKLOG.md:643`, `(akf)`); the `-n auto` figures are 338 s / 343 s (`PERFORMANCE.md:552`) and
  289 s today (`Makefile:205`), and the command is now `make e2e-fast`.
- `:68`, "`main.tsx` reads it with no cast": three `as unknown as` window-boundary casts at
  `main.tsx:98`, `:231`, `:238`. The contract types themselves are read without a cast.
- `:91-92`: 122 open entries are 124, 3,575 tests are 3,577, 1,007 in the lane are 1,014, 321
  tracked `.md` are 324 (§1's commands).
- `:33`, "8 in the rail": correct for `<nav id="sidebar">` (`index.html:43-113`, 8 `<svg>`); the
  template holds 9, the ninth being the collapse chevron at `:39` outside the nav.
- `:128-131`, `(akb)` "unchanged by any of today's work": two of its guards now read the island's
  files - `test_the_browser_holds_no_folder_wording.py:37-40` and
  `test_timeline_membership_is_not_a_label_test.py:209-213`.
- `:125`, "Nothing is red": true again as of `c40607f`; `ce59f18` was red by design for seventeen
  minutes of history and is green on `main`.

## 8. The first thing to do tomorrow

Run `make e2e-fast` on `c40607f` before anything else. Two card strings changed in that commit
(`preview.tsx:229-231`, `:301-302`) and the promise payload's tally moved to the destination's
view; every affected e2e assertion was checked by grep, not by a run, and the nightly's verdict on
this commit is not in yet. Five minutes buys the referee's answer; everything in §5 is judged
against a green lane.
