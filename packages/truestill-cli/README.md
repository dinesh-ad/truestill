# truestill-cli

The command-line interface for [vaeon](https://github.com/dinesh-ad/vaeon). A thin wrapper
over `truestill-core` that exposes the `vaeon` console command. See the repository root README
for usage.
