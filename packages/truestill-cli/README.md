# truestill-cli

The command-line interface for [Truestill](https://github.com/dinesh-ad/truestill). A thin wrapper
over `truestill-core` that exposes the `truestill` console command. See the repository root README
for usage.
